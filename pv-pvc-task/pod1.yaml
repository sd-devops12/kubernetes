apiVersion: v1
kind: Pod
metadata:
  name: pod1
spec:
  containers:
    - name: nginx-container
      image: nginx
      volumeMounts:
        - name: html-volume
          mountPath: /var/www/html
  volumes:
    - name: html-volume
      persistentVolumeClaim:
        claimName: my-pvc
